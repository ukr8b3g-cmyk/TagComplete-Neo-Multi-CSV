# Translation files

Place translation/alias CSV files in this folder. Subfolders are supported.

Recommended format:

```csv
tag,ja,aliases
long_hair,"長髪,ロングヘア","髪が長い"
```

Generic `tag,translation,aliases` and legacy two-column `tag,translation` files
are also supported.
